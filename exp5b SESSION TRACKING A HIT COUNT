Step 1: Define a servlet class PageHitCounter that extends HttpServlet. 
Step 2: Declare a global integer variable hitCount to store the total number of hits. 
Step 3: Override the init() method and initialize hitCount to 0. 
This method is executed once when the servlet is first loaded by the server. 
Step 4: Override the doGet() method to handle HTTP GET requests. 
Step 5: Set the content type of the response to "text/html" using response.set 
ContentType(). 
Step 6: Increment the hitCount variable each time the servlet is accessed. 
Step 7: Get the PrintWriter object from the response to write output to the web browser. 
Step 8: Generate an HTML response using PrintWriter that displays: 
 A heading for the title. 
 The current hit count value. 
 Step 9: Override the destroy() method if needed to store the final hit count in a database 
or log file when the servlet is being taken out of service. 
 Step 10: Configure the servlet in web.xml using:<servlet> tag to define the servlet name 
and class. 
 <servlet-mapping> tag to define the URL pattern /PageHitCounter. 

PageHitCounter.java
import java.io.IOException;
import java.io.PrintWriter;
import javax.servlet.ServletException;
import javax.servlet.http.HttpServlet;
import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;

public class PageHitCounter extends HttpServlet {

    private int hitCount;

    public void init() {
        // Reset hit counter
        hitCount = 0;
    }

    public void doGet(HttpServletRequest request, HttpServletResponse response)
            throws ServletException, IOException {

        // Set response content type
        response.setContentType("text/html");

        // Increment hit counter
        hitCount++;

        PrintWriter out = response.getWriter();
        String title = "Total Number of Hits";

        String docType = "<!DOCTYPE html>\n";

        out.println(docType +
                "<html>\n" +
                "<head><title>" + title + "</title></head>\n" +
                "<body bgcolor=\"#f0f0f0\">\n" +
                "<h1 align=\"center\">" + title + "</h1>\n" +
                "<h2 align=\"center\">" + hitCount + "</h2>\n" +
                "</body>\n" +
                "</html>");
    }

    public void destroy() {
        // Optional: Save hitCount to database here
    }
}

web.xml
<web-app>
    
    <servlet>
        <servlet-name>PageHitCounter</servlet-name>
        <servlet-class>PageHitCounter</servlet-class>
    </servlet>

    <servlet-mapping>
        <servlet-name>PageHitCounter</servlet-name>
        <url-pattern>/PageHitCounter</url-pattern>
    </servlet-mapping>

</web-app>


